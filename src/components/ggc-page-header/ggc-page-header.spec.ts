import { ggPageHeader } from './ggc-page-header';

describe('my-component', () => {
  it('should build', () => {
    expect(new ggCopyBox()).toBeTruthy();
  });

  describe('rendering', () => {

  });
});
