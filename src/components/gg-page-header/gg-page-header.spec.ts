import { ggPageHeader } from './gg-page-header';

describe('my-component', () => {
  it('should build', () => {
    expect(new ggCopyBox()).toBeTruthy();
  });

  describe('rendering', () => {

  });
});
